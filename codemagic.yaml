workflows:
  ios-app:
    name: TodoNotes iOS
    max_build_duration: 60
    environment:
      xcode: latest
      node: 18
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Build web assets
        script: |
          npm run build
      - name: Copy to iOS
        script: |
          npx cap copy ios
      - name: Install CocoaPods
        script: |
          cd ios && pod install
      - name: Build iOS
        script: |
          xcode-project build-ipa --workspace ios/App.xcworkspace --scheme App
    artifacts:
      - build/ios/ipa/*.ipa
